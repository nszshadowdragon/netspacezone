import React from 'react';

export default function StatusList() {
  return (
    <div style={{ color: '#ccc' }}>
      <p>Status Feed (Test Only)</p>
      <div style={{
        marginTop: '1rem',
        background: '#222',
        padding: '0.5rem',
        borderRadius: '6px'
      }}>
        <p>User XYZ: Hello world!</p>
      </div>
    </div>
  );
}
